apiVersion: batch/v1
kind: Job
metadata:
  name: shard-move-instructions
spec:
  template:
    spec:
      containers:
      - name: shard-move-instructions
        image: public.ecr.aws/medic/couchdb-migration:1.0.2
        command: ["/bin/sh", "-c"]
        args: ["shard-move-instructions $(cat /data/shard-matrix)"]
        volumeMounts:
        - name: data
          mountPath: /data
        env:
        - name: COUCH_URL
          valueFrom:
            secretKeyRef:
              name: cht-secrets
              key: COUCH_URL
        envFrom:
        - configMapRef:
            name: env-config
        - secretRef:
            name: env-secrets
      restartPolicy: Never
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: shard-matrix-pvc
